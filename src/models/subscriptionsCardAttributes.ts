/**
 * PayPal Server SDKLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema } from '../schema.js';
import { CardCustomer, cardCustomerSchema } from './cardCustomer.js';
import {
  CardVerification,
  cardVerificationSchema,
} from './cardVerification.js';
import {
  VaultInstructionBase,
  vaultInstructionBaseSchema,
} from './vaultInstructionBase.js';

/** Additional attributes associated with the use of this card. */
export interface SubscriptionsCardAttributes {
  /** The details about a customer in PayPal's system of record. */
  customer?: CardCustomer;
  /** Basic vault instruction specification that can be extended by specific payment sources that supports vaulting. */
  vault?: VaultInstructionBase;
  /** The API caller can opt in to verify the card through PayPal offered verification services (e.g. Smart Dollar Auth, 3DS). */
  verification?: CardVerification;
}

export const subscriptionsCardAttributesSchema: Schema<SubscriptionsCardAttributes> = object(
  {
    customer: ['customer', optional(lazy(() => cardCustomerSchema))],
    vault: ['vault', optional(lazy(() => vaultInstructionBaseSchema))],
    verification: [
      'verification',
      optional(lazy(() => cardVerificationSchema)),
    ],
  }
);
