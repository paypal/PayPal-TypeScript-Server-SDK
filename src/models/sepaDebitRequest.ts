/**
 * PayPal Server SDKLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema } from '../schema.js';
import {
  SepaDebitExperienceContext,
  sepaDebitExperienceContextSchema,
} from './sepaDebitExperienceContext.js';

/** An API resource denoting a request to securely store a SEPA Debit. */
export interface SepaDebitRequest {
  /** Customizes the payer experience during the approval process for the SEPA Debit payment. */
  experienceContext?: SepaDebitExperienceContext;
}

export const sepaDebitRequestSchema: Schema<SepaDebitRequest> = object({
  experienceContext: [
    'experience_context',
    optional(lazy(() => sepaDebitExperienceContextSchema)),
  ],
});
